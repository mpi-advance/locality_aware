message (STATUS "AT INTERNAL INCLUDES")
include_directories(collective)
include_directories(communicator)
include_directories(neighborhood)
include_directories(persistent)
if (USE_GPU)
	message(STATUS "GPU SET")
	if(HIP)
		message(STATUS "HIP FLAG TRIPPED")
	endif()
	if(USE_CUDA)
		message(STATUS "CUDA FLAG TRIPPED")
	endif()
	
	add_subdirectory(utils)
endif()